
-- PLACE any SQL statements required after the database migration here
-- Truncate tables that do not need to be on the cloned host,
-- grant access rights etc.

-- Change sql_mode to the database in order for the inserts to work (mysql 5.6 fix)
SET GLOBAL sql_mode = 'NO_ENGINE_SUBSTITUTION';

